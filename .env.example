# Inside IR Ferrovial needs NO environment variables and NO API keys.
#
# Live market data is fetched by a scheduled GitHub Action (.github/workflows/
# refresh-data.yml) which runs Yahoo Finance from GitHub's Azure runners — where
# Yahoo is NOT IP-blocked — and commits the results as static JSON under
# /data/live. Vercel just serves those static files.
#
# Nothing to configure here.
